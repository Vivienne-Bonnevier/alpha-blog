<% @categories.each do |category| %>
  <div class="rounded-xl max-w-sm bg-white mx-auto my-2">
    <p class = "text-center underline">
      <%= link_to category.name, category_path(category) %>
    </p>
  </div>
<% end %>