<!-- Error checking -->
<% if obj.errors.any? %>
  <p>Please resolve all errors to continue.</p>
  <ul>
    <% obj.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>